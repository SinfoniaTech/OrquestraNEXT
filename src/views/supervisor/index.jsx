/**
 * Fachada que transforma a aplicação Supervisor em uma cena do
 * controlador de apresentações.
 *
 * Uma cena precisa expor um componente React como export default no
 * index.jsx da pasta — este arquivo apenas repassa o componente raiz da
 * aplicação, sem duplicar nem alterar nada dentro dela.
 */
export { default } from './SupervisorView.jsx';
