/**
 * Visualização "Visão Geral" — aba mantida na barra inferior, mas sem
 * conteúdo por enquanto. Substituir pela aplicação real quando ela for
 * implementada.
 */
export function VisaoGeral() {
  return <div />;
}
