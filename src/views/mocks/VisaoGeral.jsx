import { MockView } from './MockView.jsx';

/**
 * Mock "Visão Geral" — placeholder apenas para demonstrar a navegação.
 * Substituir pela aplicação real quando ela for implementada.
 */
export function VisaoGeral() {
  return (
    <MockView
      title="Visão Geral"
      description="Visualização de demonstração. Este espaço será ocupado pela aplicação real de Visão Geral. Clique nas abas da barra inferior — ou use ← e → no teclado — para navegar entre as visualizações sem recarregar a página."
      accent="#2f6fed"
    />
  );
}
