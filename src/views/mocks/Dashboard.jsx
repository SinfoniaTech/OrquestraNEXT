/**
 * Visualização "Dashboard" — aba mantida na barra inferior, mas sem
 * conteúdo por enquanto. Substituir pela aplicação real quando ela for
 * implementada.
 */
export function Dashboard() {
  return <div />;
}
