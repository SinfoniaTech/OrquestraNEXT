import { MockView } from './MockView.jsx';

/**
 * Mock "Dashboard" — placeholder apenas para demonstrar a navegação.
 * Substituir pela aplicação real quando ela for implementada.
 */
export function Dashboard() {
  return (
    <MockView
      title="Dashboard"
      description="Visualização de demonstração. Este espaço será ocupado pela aplicação real de Dashboard. O container não impõe nenhum layout às páginas internas — cada visualização terá estrutura, estilos e interações próprios."
      accent="#0c8f66"
    />
  );
}
